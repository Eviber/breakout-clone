[package]
name = "breakout-clone"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.19.0", features = ["bevy_feathers", "debug", "dynamic_linking"] }
bevy-inspector-egui = "0.37"
